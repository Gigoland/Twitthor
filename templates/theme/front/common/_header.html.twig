<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_index') }}">Twit<string>thor</strong>.com</a>

    <div class="collapse navbar-collapse" id="navbarNav">

      <ul class="navbar-nav mr-auto">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_admin_dashboard') }}">Dashboard</a>
          </li>
         {% endif %}
      </ul>

      <ul class="navbar-nav">
        {% if app.user %}
          <li class="nav-item dropdown ms-md-auto">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">{{ app.user.twUsername|default(app.user.email) }}</a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                  <a class="dropdown-item" href="{{ path('app_security_signout') }}">Sign Out</a>
                </li>
              {% endif %}
            </ul>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_security_signin') }}">Sign In / Sign Up</a>
          </li>
        {% endif %}
      </ul>

    </div>

  </div>
</nav>
