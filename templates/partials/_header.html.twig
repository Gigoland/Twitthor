<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_index') }}">Twit<string>thor</strong>.com</a>

    <div class="collapse navbar-collapse" id="navbarNav">

      <ul class="navbar-nav mr-auto">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_following') }}">Following</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_followers') }}">Followers</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_outers') }}">Outers</a>
          </li>
        {% endif %}
      </ul>

      <ul class="navbar-nav">
        {% if app.user %}
          <li class="nav-item dropdown ms-md-auto">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">{{ app.user.twUsername }}</a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <li>
                <a class="dropdown-item" href="{{ path('app_api_keys') }}">Twitter config</a>
              </li>
              <li>
                <hr class="dropdown-divider">
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('app_edit_my_profile') }}">Edit Profile</a>
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('app_edit_my_password') }}">Change your password</a>
              </li>
              <li>
                <hr class="dropdown-divider">
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('app_security_logout') }}">Logout</a>
              </li>
            </ul>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_security_login') }}">Login / Registration</a>
          </li>
        {% endif %}
      </ul>

    </div>

  </div>
</nav>
